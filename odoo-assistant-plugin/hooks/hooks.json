{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import os,sys,subprocess; root=os.environ.get('CLAUDE_PLUGIN_ROOT') or r'${CLAUDE_PLUGIN_ROOT}'; p=os.path.join(root,'hooks','skill_plugin_hygiene.py'); sys.exit(subprocess.run([sys.executable,p]).returncode if os.path.isfile(p) else 0)\"",
            "timeout": 15,
            "statusMessage": "Skill/plugin hygiene check"
          }
        ]
      }
    ]
  }
}
